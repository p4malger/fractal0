# 🚀 دليل رفع الموقع على Vercel — خطوة بخطوة

> هذا الدليل مكتوب للمبتدئين. اتبع الخطوات بالترتيب ولا تقلق — **كل شيء مجاني تمامًا**.
> ستحصل في النهاية على موقع يعمل على رابط مثل: `your-project.vercel.app`
>
> المدة التقديرية: **15–20 دقيقة**

---

## 📋 ما ستحتاجه قبل البدء

| الحساب | الرابط | ملاحظة |
|---|---|---|
| حساب GitHub | [github.com](https://github.com) | لرفع الكود |
| حساب Vercel | [vercel.com](https://vercel.com) | لاستضافة الموقع |
| حساب Neon | [neon.tech](https://neon.tech) | قاعدة البيانات المجانية |

> 💡 يمكنك تسجيل الدخول في Vercel و Neon مباشرة بحساب GitHub نفسه — لا حاجة لكلمات مرور جديدة.

---

## الخطوة 1️⃣ — رفع الكود على GitHub

### الطريقة الأسهل (من المتصفح — بدون تثبيت أي برنامج):

1. سجّل الدخول إلى [github.com](https://github.com)
2. اضغط الزر الأخضر **`+`** في الأعلى ← ثم **New repository**
3. اكتب اسم المشروع، مثلاً: `gohealthy-formation`
4. اختر **Private** (خاص) أو **Public** — كلاهما يعمل مع Vercel
5. اضغط **Create repository**
6. في صفحة المستودع الجديد، اضغط على **uploading an existing file** (رابط أزرق في منتصف الصفحة)
7. **اسحب كل محتويات مجلد المشروع** (بعد فك الضغط عن ملف ZIP) إلى نافذة الرفع
   - ⚠️ **تأكد من رفع الملفات مباشرة وليس المجلد المضغوط**: يجب أن يظهر ملف `package.json` في جذر المستودع
   - ملاحظة: ملف `.env` **لن يُرفع** لأنه مستثنى في `.gitignore` — هذا مقصود وفية آمانك ✅
8. اضغط **Commit changes** وانتظر انتهاء الرفع

### طريقة بديلة (برنامج GitHub Desktop — أسهل للتحديثات اللاحقة):

حمّل [GitHub Desktop](https://desktop.github.com)، سجّل الدخول، ثم:
**File → Add local repository** (أو Clone) وارفع المشروع كما شرح أعلاه.

---

## الخطوة 2️⃣ — إنشاء قاعدة البيانات (Neon) المجانية

قاعدة البيانات هي المكان الذي تُحفظ فيه **أسماء المسجلين** وإعدادات الموقع.

1. اذهب إلى [neon.tech](https://neon.tech) واضغط **Sign Up** (استخدم حساب GitHub)
2. بعد الدخول: اضغط **Create project** (أو **New Project**)
   - اسم المشروع: اكتب مثلاً `gohealthy`
   - Region: اختر **Frankfurt (aws eu-central-1)** — الأقرب للجزائر
3. بعد إنشاء المشروع ستظهر لك نافذة **Connection String** — ابحث عن الرابط الذي يبدأ بـ:
   ```
   postgresql://user:password@ep-xxxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
   ```
4. ⚠️ **مهم جداً**: انسخ الرابط الذي يحتوي على كلمة **`-pooler`** (هذا الرابط المتوافق مع Vercel)
5. احفظ هذا الرابط مؤقتًا في ملف نصي على جهازك — ستحتاجه في الخطوة التالية

> 💡 لا تقلق بشأن `-pooler` والترحيلات: أمر البناء في المشروع **يحوّل الرابط تلقائيًا** إلى الرابط المباشر عند إنشاء الجداول، ثم يعود للرابط المُجمّع في التشغيل — كل شيء تلقائي.
>
> 💡 إذا لم تجد الرابط: من لوحة Neon اضغط **Dashboard** ثم **Connect** واختر **Pooled connection**.

---

## الخطوة 3️⃣ — ربط المشروع بـ Vercel

1. اذهب إلى [vercel.com](https://vercel.com) واضغط **Sign Up with GitHub**
2. من لوحة التحكم اضغط **Add New... → Project**
3. سترى قائمة مستودعات GitHub — اضغط **Import** بجانب مستودع `gohealthy-formation`
4. ⚠️ **قبل الضغط على Deploy** — نضيف متغيّري البيئة (هذه هي "الربط" مع قاعدة البيانات):

   في قسم **Environment Variables** أضف المتغيرين التاليين:

   | Name | Value |
   |---|---|
   | `DATABASE_URL` | الرابط الذي نسخته من Neon (الخطوة 2) |
   | `ADMIN_PASSWORD` | كلمة مرور جديدة خاصة بك لصفحة `/admin` |

5. اضغط **Deploy** وانتظر 2–3 دقائق ⏳

> 🎉 أثناء البناء، يقوم المشروع **تلقائيًا** بإنشاء جداول قاعدة البيانات (`prisma migrate deploy`) — لا تحتاج لكتابة أي أوامر!

---

## الخطوة 4️⃣ — التحقق من أن كل شيء يعمل

بعد انتهاء النشر ستحصل على رابط مثل `https://gohealthy-formation.vercel.app`:

### ✅ اختبار الموقع:
1. افتح الرابط — يجب أن تظهر صفحة الدورة كاملة
2. جرّب نموذج التسجيل: املأ البيانات وأرسل

### ✅ اختبار لوحة التحكم:
1. افتح الرابط مع إضافة `/admin` في النهاية:
   ```
   https://your-project.vercel.app/admin
   ```
2. أدخل كلمة المرور التي اخترتها في `ADMIN_PASSWORD`
3. يجب أن ترى تسجيلك التجريبي في قائمة المسجلين 🎊

---

## 🔧 بعد النشر — كيف تدير موقعك؟

| ماذا تريد أن تفعل؟ | كيف؟ |
|---|---|
| تغيير روابط الفيديوهات والصور | لوحة `/admin` ← تبويب **الوسائط** — بدون كود |
| رؤية المسجلين الجدد وتصديرهم CSV | لوحة `/admin` — زر **تصدير CSV** |
| تغيير الأسعار أو النصوص أو التواريخ | تعديل ملف `src/lib/course-data.ts` ثم رفع التعديل على GitHub (Vercel يعيد النشر تلقائيًا) |
| تغيير كلمة مرور `/admin` | Vercel ← Settings ← Environment Variables ← عدّل `ADMIN_PASSWORD` ← **Redeploy** |

### تحديث الكود مستقبلاً:
عند تعديل أي ملف ورفعه إلى GitHub (Commit)، يقوم Vercel **تلقائيًا** بإعادة نشر الموقع خلال دقيقة.

---

## ❓ حل المشاكل الشائعة

| المشكلة | الحل |
|---|---|
| صفحة `/admin` لا تقبل كلمة المرور | تأكد من قيمة `ADMIN_PASSWORD` في إعدادات Vercel، ثم اضغط **Redeploy** |
| رسالة خطأ تتعلق بقاعدة البيانات | تحقق من نسخ رابط Neon **المحتوي على `-pooler`** بالكامل في `DATABASE_URL` |
| التعديلات على GitHub لا تظهر | من Vercel: **Deployments ← آخر نشر ← Redeploy** |
| فشل البناء (Build Failed) مع `Could not find a schema.prisma` | الملف `prisma/schema.prisma` غير موجود في مكانه الصحيح — يجب أن يكون داخل مجلد `prisma` وليس في الجذر |
| فشل البناء مع `P1001: Can't reach database server` | `DATABASE_URL` غير مضبوط في Vercel أو رابط Neon منسوخ ناقص — أعد نسخه كاملًا |
| رفض GitHub رفع ملف `.prisma` عبر المتصفح | استعمل **GitHub Desktop** (الأسهل) أو ارفع مجلد `prisma` كاملًا بالسحب، أو انسخ محتوى الملف والصقه عبر **Add file → Create new file** باسم `schema.prisma` داخل مجلد `prisma` |

---

## 🔒 ملاحظات أمنية

- ملف `.env` **لا يُرفع أبدًا** إلى GitHub (مستثنى تلقائيًا في `.gitignore`) — أسرارك تبقى في Vercel فقط
- كلمة مرور `/admin` الافتراضية مؤقتة — **غيّرها قبل نشر الموقع للجمهور**
- قاعدة Neon المجانية تكفي لمئات التسجيلات، ويمكن ترقيتها لاحقًا مجانًا عند الحاجة
